# ── FileAI — dipendenze ──────────────────────────────────────────

# Core
rich>=13.0
requests>=2.28

# Backend AI (necessario almeno uno — installati tutti per non bloccare l'avvio)
ollama>=0.4.0
anthropic>=0.34.0

# GUI dark-mode (PyQt6 include QtSvg, usato per le icone)
PyQt6>=6.5

# Lettura PDF nell'analisi semantica (opzionale, raccomandato)
pypdf>=4.0

# ── Build (opzionale) ────────────────────────────────────────────
# Decommenta per generare un eseguibile standalone con PyInstaller
# pyinstaller>=6.0
# pillow>=10.0          # serve per la conversione automatica icone su Windows
